{
  "pluginAlias": "PipoSpeak",
  "pluginType": "platform",
  "singular": true,
  "footerDisplay": "Playback uses the [homebridge-homepod-radio](https://www.npmjs.com/package/homebridge-homepod-radio) `/play` route automatically when that plugin is installed; otherwise it streams directly with **pyatv** (`atvremote`). The LAN HTTP endpoint is **off by default** — only enable it on a trusted network (or set an access token).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "Pipo Speak",
        "required": true,
        "description": "Shown in the Homebridge log only."
      },
      "buttons": {
        "title": "Phrase Buttons",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": {
              "title": "Button Name",
              "type": "string",
              "required": true,
              "placeholder": "Dinner is ready"
            },
            "phrase": {
              "title": "Phrase to Speak",
              "type": "string",
              "required": true,
              "placeholder": "Dinner is ready, come to the kitchen."
            },
            "volumeOverride": {
              "title": "Override volume for this phrase",
              "type": "boolean",
              "default": false
            },
            "volume": {
              "title": "Volume (0-100)",
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "placeholder": "75",
              "condition": {
                "functionBody": "return Array.isArray(model.buttons) && model.buttons[arrayIndices[0]] && model.buttons[arrayIndices[0]].volumeOverride === true;"
              }
            },
            "voice": {
              "title": "Voice",
              "type": "string",
              "default": "default",
              "description": "Use the configured default voice, or always try a specific Azure or local voice first. If that voice fails, other configured voices are tried and the details are written to the Homebridge log.",
              "oneOf": [
                {
                  "title": "Use default voice",
                  "enum": ["default"]
                },
                {
                  "title": "Azure: Ava - US English female (HD, most natural)",
                  "enum": ["azure:en-US-Ava:DragonHDLatestNeural"]
                },
                {
                  "title": "Azure: Andrew - US English male (HD)",
                  "enum": ["azure:en-US-Andrew:DragonHDLatestNeural"]
                },
                {
                  "title": "Azure: Emma - US English female (HD)",
                  "enum": ["azure:en-US-Emma:DragonHDLatestNeural"]
                },
                {
                  "title": "Azure: Brian - US English male (HD)",
                  "enum": ["azure:en-US-Brian:DragonHDLatestNeural"]
                },
                {
                  "title": "Azure: Aria - US English female (neural, emotive)",
                  "enum": ["azure:en-US-AriaNeural"]
                },
                {
                  "title": "Azure: Jenny - US English female (neural, emotive)",
                  "enum": ["azure:en-US-JennyNeural"]
                },
                {
                  "title": "Azure: Ada - British English female (HD)",
                  "enum": ["azure:en-GB-Ada:DragonHDLatestNeural"]
                },
                {
                  "title": "Azure: Ryan - British English male (HD)",
                  "enum": ["azure:en-GB-Ryan:DragonHDLatestNeural"]
                },
                {
                  "title": "Local: Lessac - US English (light)",
                  "enum": ["piper:en_US-lessac-low"]
                },
                {
                  "title": "Local: Amy - US English female (light)",
                  "enum": ["piper:en_US-amy-low"]
                },
                {
                  "title": "Local: Ryan - US English male (light)",
                  "enum": ["piper:en_US-ryan-low"]
                },
                {
                  "title": "Local: Kathleen - US English female (light)",
                  "enum": ["piper:en_US-kathleen-low"]
                },
                {
                  "title": "Local: Danny - US English male (light)",
                  "enum": ["piper:en_US-danny-low"]
                },
                {
                  "title": "Local: Alan - British English male (light)",
                  "enum": ["piper:en_GB-alan-low"]
                },
                {
                  "title": "Local: Lessac - US English (richer)",
                  "enum": ["piper:en_US-lessac-medium"]
                },
                {
                  "title": "Local: Amy - US English female (richer)",
                  "enum": ["piper:en_US-amy-medium"]
                }
              ]
            },
            "speed": {
              "title": "Speed for this phrase (optional)",
              "type": "number",
              "minimum": 0.5,
              "maximum": 2,
              "placeholder": "1.0",
              "description": "Leave blank to use the default speed. 1.0 = normal, 2.0 = twice as fast, 0.5 = half speed."
            },
            "atvId": {
              "title": "Speaker for this phrase (optional)",
              "type": "string",
              "placeholder": "AA:BB:CC:DD:EE:FF",
              "description": "Optional pyatv device ID to play this one phrase on a specific speaker/room. Leave blank to use the default playback target."
            }
          }
        }
      },
      "defaultVolume": {
        "title": "Default Volume (0-100)",
        "type": "integer",
        "default": 75,
        "minimum": 0,
        "maximum": 100
      },
      "speed": {
        "title": "Default Speech Speed",
        "type": "number",
        "default": 1,
        "minimum": 0.5,
        "maximum": 2,
        "description": "1.0 = normal, 2.0 = twice as fast, 0.5 = half speed. Applied to every phrase unless a button overrides it."
      },
      "playback": {
        "title": "Playback Target",
        "type": "string",
        "default": "auto",
        "description": "Auto-detects homebridge-homepod-radio and routes audio through it when installed; otherwise streams directly with pyatv. See the link at the bottom of this page.",
        "oneOf": [
          { "title": "Use HomePod Radio if installed", "enum": ["auto"] },
          { "title": "Stream directly with pyatv", "enum": ["pyatv"] }
        ]
      },
      "atvId": {
        "title": "pyatv Device ID",
        "type": "string",
        "placeholder": "AA:BB:CC:DD:EE:FF",
        "description": "Identifier of the target speaker for pyatv (from `atvremote scan`). Required for the pyatv playback target.",
        "condition": {
          "functionBody": "return model.playback !== 'homepod-radio';"
        }
      },
      "voice": {
        "title": "Default Voice",
        "type": "string",
        "default": "piper:en_US-lessac-low",
        "description": "Used by every phrase button set to Use default voice. Azure voices require Azure Cloud Voice below; local voices run with Piper on the device.",
        "oneOf": [
          {
            "title": "Azure: Ava - US English female (HD, most natural)",
            "enum": ["azure:en-US-Ava:DragonHDLatestNeural"]
          },
          {
            "title": "Azure: Andrew - US English male (HD)",
            "enum": ["azure:en-US-Andrew:DragonHDLatestNeural"]
          },
          {
            "title": "Azure: Emma - US English female (HD)",
            "enum": ["azure:en-US-Emma:DragonHDLatestNeural"]
          },
          {
            "title": "Azure: Brian - US English male (HD)",
            "enum": ["azure:en-US-Brian:DragonHDLatestNeural"]
          },
          {
            "title": "Azure: Aria - US English female (neural, emotive)",
            "enum": ["azure:en-US-AriaNeural"]
          },
          {
            "title": "Azure: Jenny - US English female (neural, emotive)",
            "enum": ["azure:en-US-JennyNeural"]
          },
          {
            "title": "Azure: Ada - British English female (HD)",
            "enum": ["azure:en-GB-Ada:DragonHDLatestNeural"]
          },
          {
            "title": "Azure: Ryan - British English male (HD)",
            "enum": ["azure:en-GB-Ryan:DragonHDLatestNeural"]
          },
          {
            "title": "Local: Lessac - US English (light)",
            "enum": ["piper:en_US-lessac-low"]
          },
          {
            "title": "Local: Amy - US English female (light)",
            "enum": ["piper:en_US-amy-low"]
          },
          {
            "title": "Local: Ryan - US English male (light)",
            "enum": ["piper:en_US-ryan-low"]
          },
          {
            "title": "Local: Kathleen - US English female (light)",
            "enum": ["piper:en_US-kathleen-low"]
          },
          {
            "title": "Local: Danny - US English male (light)",
            "enum": ["piper:en_US-danny-low"]
          },
          {
            "title": "Local: Alan - British English male (light)",
            "enum": ["piper:en_GB-alan-low"]
          },
          {
            "title": "Local: Lessac - US English (richer)",
            "enum": ["piper:en_US-lessac-medium"]
          },
          {
            "title": "Local: Amy - US English female (richer)",
            "enum": ["piper:en_US-amy-medium"]
          }
        ]
      },
      "chimeFile": {
        "title": "Attention Chime (optional)",
        "type": "string",
        "placeholder": "/var/www/tones/chime.wav",
        "description": "Absolute path to a short WAV played right before each phrase. Leave blank for none."
      },
      "azure": {
        "title": "Azure Cloud Voice (high quality)",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Enable Azure cloud voices",
            "type": "boolean",
            "default": false,
            "description": "Enables synthesis for Azure choices in the Default Voice and phrase-button Voice selectors. Azure failures are logged and fall back to another configured voice so a button press still has the best chance to speak."
          },
          "region": {
            "title": "Azure Region",
            "type": "string",
            "default": "eastus",
            "placeholder": "eastus",
            "description": "Region of your Azure Speech resource (e.g. eastus). Sets the endpoint https://<region>.tts.speech.microsoft.com.",
            "condition": {
              "functionBody": "return model.azure && model.azure.enabled === true;"
            }
          },
          "key": {
            "title": "Azure Speech Key",
            "type": "string",
            "description": "Subscription key (KEY 1 or KEY 2) of your Azure Speech resource. Stored in this config; or leave blank and set the PIPO_SPEAK_AZURE_KEY environment variable on the Homebridge service so the secret stays out of config.json.",
            "condition": {
              "functionBody": "return model.azure && model.azure.enabled === true;"
            }
          }
        }
      },
      "enableHttp": {
        "title": "Enable LAN HTTP \"speak anything\" endpoint",
        "type": "boolean",
        "default": false,
        "description": "When on, exposes POST /say (and GET /say?text=...) so other devices on your network can speak arbitrary text. Keep it on a trusted LAN, and/or set an access token below."
      },
      "httpPort": {
        "title": "HTTP Port",
        "type": "integer",
        "default": 8095,
        "minimum": 1,
        "maximum": 65535,
        "condition": {
          "functionBody": "return model.enableHttp === true;"
        }
      },
      "httpToken": {
        "title": "HTTP Access Token (optional)",
        "type": "string",
        "description": "When set, every /say request must present this token via ?token=, an Authorization: Bearer header, or X-Auth-Token. /healthz stays open. Leave blank for no auth (trusted LAN only).",
        "condition": {
          "functionBody": "return model.enableHttp === true;"
        }
      },
      "cacheEnabled": {
        "title": "Cache synthesized phrases",
        "type": "boolean",
        "default": true,
        "description": "Replay a repeated phrase from a cached WAV instead of re-synthesizing it. Strongly recommended on small boards — it removes the on-demand synthesis (and its memory spike) from the common path."
      },
      "preRender": {
        "title": "Pre-render phrases on startup",
        "type": "boolean",
        "default": false,
        "description": "Synthesize and cache every button phrase once in the background at startup, so the first press is instant. Paced so it won't overload a small board. Requires the cache."
      },
      "restoreVolume": {
        "title": "Restore speaker volume after announcement (pyatv only)",
        "type": "boolean",
        "default": false,
        "description": "For the pyatv playback target, read the speaker's current volume before an announcement and restore it afterwards. Adds two extra pyatv calls per utterance."
      },
      "cacheMaxEntries": {
        "title": "Phrase Cache Size (entries)",
        "type": "integer",
        "default": 64,
        "minimum": 1,
        "maximum": 1000,
        "description": "Maximum number of cached phrase WAVs kept on disk (oldest are evicted first)."
      },
      "cacheDir": {
        "title": "Persistent Phrase Cache Directory",
        "type": "string",
        "placeholder": "/var/lib/homebridge/pipo-speak-cache",
        "description": "Optional absolute directory for cached WAVs and quality metadata. Set this outside the installed plugin directory so phrases survive npm/plugin upgrades. The Homebridge service user must be able to write it. PIPO_SPEAK_CACHE_DIR remains an environment-variable alternative."
      },
      "maxChars": {
        "title": "Max Characters Per Utterance",
        "type": "integer",
        "default": 600,
        "minimum": 1,
        "maximum": 4000
      },
      "minAvailableMb": {
        "title": "Minimum Free RAM (MiB) Before Synthesizing",
        "type": "integer",
        "default": 90,
        "minimum": 0,
        "description": "Pre-flight memory gate. If MemAvailable is below this, the request is refused instead of risking an out-of-memory reboot on small boards. Set 0 to disable. (Cache hits are never gated.)"
      },
      "cooldownSeconds": {
        "title": "Cooldown Between Utterances (seconds)",
        "type": "number",
        "default": 4,
        "minimum": 0
      },
      "piperThreads": {
        "title": "Piper Thread Cap",
        "type": "integer",
        "minimum": 1,
        "maximum": 8,
        "placeholder": "(default)",
        "description": "Optional. Fewer threads = lower CPU/memory spike, slower synthesis. Recommended: 1 on a single-core / low-RAM board."
      },
      "soundboard": {
        "title": "Soundboard",
        "type": "object",
        "properties": {
          "enabled": {
            "title": "Enable Soundboard",
            "type": "boolean",
            "default": false,
            "description": "Expose a HomeKit Television whose inputs are the first audio files found in a folder. Selecting an input plays that sound on your speaker."
          },
          "name": {
            "title": "Soundboard Name",
            "type": "string",
            "default": "Soundboard",
            "placeholder": "Soundboard",
            "description": "Name of the Television accessory shown in the Home app.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "sourceFolder": {
            "title": "Soundboard Source Folder",
            "type": "string",
            "placeholder": "/var/www/tones/sample-gallery",
            "description": "Absolute path to a folder on the Pi. If Sound Files is empty, the first sounds found (depth-first) become the TV's inputs; otherwise Sound Files are resolved relative to this folder.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "soundFiles": {
            "title": "Soundboard Sound Files (optional)",
            "type": "array",
            "description": "Optional ordered allowlist of audio files to expose as this TV's inputs. Use paths relative to the Source Folder, for example mac-system-sounds/Basso.aiff. Missing, non-audio, or outside-folder paths are skipped.",
            "items": {
              "type": "string",
              "placeholder": "mac-system-sounds/Basso.aiff"
            },
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "maxSounds": {
            "title": "Maximum Inputs",
            "type": "integer",
            "default": 10,
            "minimum": 1,
            "maximum": 10,
            "description": "How many auto-discovered sounds to expose as inputs when Sound Files is empty (plus the synthetic 'None'). HomeKit limits a TV to a small number of inputs.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "volume": {
            "title": "Soundboard Volume (0-100)",
            "type": "integer",
            "minimum": 0,
            "maximum": 100,
            "placeholder": "(default volume)",
            "description": "Optional volume for soundboard playback. Leave blank to use the default volume.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "atvId": {
            "title": "Soundboard Speaker (optional)",
            "type": "string",
            "placeholder": "AA:BB:CC:DD:EE:FF",
            "description": "Optional pyatv device ID to play the soundboard on a specific speaker. Leave blank to use the default playback target.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          },
          "warmConnection": {
            "title": "Keep a warm AirPlay connection (instant playback)",
            "type": "boolean",
            "default": true,
            "description": "Hold one pyatv connection to the soundboard speaker open so presses play almost instantly instead of paying a 5-15s cold connect each time. Requires the soundboard speaker ID above. Falls back to the normal /play route if unavailable.",
            "condition": {
              "functionBody": "return model.soundboard && model.soundboard.enabled === true;"
            }
          }
        }
      },
      "soundboards": {
        "title": "Additional Soundboards",
        "type": "array",
        "description": "Optional additional HomeKit Television soundboards published by this same PipoSpeak platform instance.",
        "items": {
          "type": "object",
          "properties": {
            "enabled": {
              "title": "Enable Soundboard",
              "type": "boolean",
              "default": true
            },
            "id": {
              "title": "Stable ID (optional)",
              "type": "string",
              "description": "Optional stable identifier used for the HomeKit accessory UUID. Leave blank to use the soundboard name."
            },
            "name": {
              "title": "Soundboard Name",
              "type": "string",
              "default": "Soundboard",
              "placeholder": "Mac System Sounds",
              "description": "Name of the Television accessory shown in the Home app."
            },
            "sourceFolder": {
              "title": "Soundboard Source Folder",
              "type": "string",
              "placeholder": "/var/www/tones",
              "description": "Absolute path to a folder on the Pi. If Sound Files is empty, the first sounds found (depth-first) become the TV's inputs; otherwise Sound Files are resolved relative to this folder."
            },
            "soundFiles": {
              "title": "Soundboard Sound Files (optional)",
              "type": "array",
              "description": "Optional ordered allowlist of audio files to expose as this TV's inputs. Use paths relative to the Source Folder.",
              "items": {
                "type": "string",
                "placeholder": "mac-system-sounds/Basso.aiff"
              }
            },
            "maxSounds": {
              "title": "Maximum Inputs",
              "type": "integer",
              "default": 10,
              "minimum": 1,
              "maximum": 10,
              "description": "How many auto-discovered sounds to expose as inputs when Sound Files is empty (plus the synthetic 'None')."
            },
            "volume": {
              "title": "Soundboard Volume (0-100)",
              "type": "integer",
              "minimum": 0,
              "maximum": 100,
              "placeholder": "(default volume)"
            },
            "atvId": {
              "title": "Soundboard Speaker (optional)",
              "type": "string",
              "placeholder": "AA:BB:CC:DD:EE:FF"
            },
            "warmConnection": {
              "title": "Keep a warm AirPlay connection (instant playback)",
              "type": "boolean",
              "default": true
            }
          }
        }
      }
    }
  },
  "layout": [
    {
      "type": "section",
      "title": "Phrase Buttons",
      "expandable": true,
      "expanded": true,
      "items": [
        {
          "key": "buttons",
          "type": "array",
          "notitle": true,
          "items": [
            "buttons[].name",
            "buttons[].phrase",
            "buttons[].volumeOverride",
            "buttons[].volume",
            "buttons[].voice",
            "buttons[].speed",
            "buttons[].atvId"
          ]
        }
      ]
    },
    {
      "type": "section",
      "title": "Playback & Voice",
      "expandable": true,
      "expanded": false,
      "items": [
        "playback",
        "defaultVolume",
        "speed",
        "atvId",
        "voice",
        "chimeFile"
      ]
    },
    {
      "type": "section",
      "title": "Azure Cloud Voice (high quality)",
      "expandable": true,
      "expanded": false,
      "items": ["azure.enabled", "azure.region", "azure.key"]
    },
    {
      "type": "section",
      "title": "LAN HTTP Endpoint",
      "expandable": true,
      "expanded": false,
      "items": [
        "enableHttp",
        { "key": "httpPort", "type": "number", "placeholder": "8095" },
        "httpToken"
      ]
    },
    {
      "type": "section",
      "title": "Performance & Startup",
      "expandable": true,
      "expanded": false,
      "items": ["cacheEnabled", "preRender", "restoreVolume", "cacheMaxEntries"]
    },
    {
      "type": "section",
      "title": "Advanced / Memory Safety",
      "expandable": true,
      "expanded": false,
      "items": ["maxChars", "minAvailableMb", "cooldownSeconds", "piperThreads"]
    },
    {
      "type": "section",
      "title": "Soundboard",
      "expandable": true,
      "expanded": false,
      "items": [
        "soundboard.enabled",
        "soundboard.name",
        "soundboard.sourceFolder",
        "soundboard.soundFiles",
        "soundboard.maxSounds",
        "soundboard.volume",
        "soundboard.atvId"
      ]
    },
    {
      "type": "section",
      "title": "Additional Soundboards",
      "expandable": true,
      "expanded": false,
      "items": [
        {
          "key": "soundboards",
          "type": "array",
          "notitle": true,
          "items": [
            "soundboards[].enabled",
            "soundboards[].id",
            "soundboards[].name",
            "soundboards[].sourceFolder",
            "soundboards[].soundFiles",
            "soundboards[].maxSounds",
            "soundboards[].volume",
            "soundboards[].atvId",
            "soundboards[].warmConnection"
          ]
        }
      ]
    }
  ]
}
